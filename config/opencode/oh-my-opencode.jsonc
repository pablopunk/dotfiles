{
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-opencode/master/assets/oh-my-opencode.schema.json",
  "agents": {
    // ============================================================
    // TIER 1: BRAINS — need maximum general intelligence
    // GLM-5: #1 open-weight (Intelligence Index 50), best agentic,
    // long-horizon planning, 76.5 tok/s
    // ============================================================

    // 🎯 Orchestrator: delegates, coordinates, maintains TODO list
    "sisyphus": {
      "model": "openrouter/moonshotai/kimi-k2.5"
    },

    // 📋 Planner: creates multi-step plans with dependency graphs
    "prometheus": {
      "model": "openai/gpt-5.2"
    },

    // 🐛 Debugger/Advisor: read-only, needs maximum IQ
    "oracle": {
      "model": "openai/gpt-5.3-codex"
    },

    // ✅ Pre-planning: detects ambiguities and hidden requirements
    "metis": {
      "model": "openai/gpt-5.2"
    },

    // ============================================================
    // TIER 2: DEEP WORKERS — deep reasoning + long context
    // Kimi K2.5: 256K context, 83.1% LiveCodeBench, 96.1% AIME,
    // multimodal, visual programming
    // ============================================================

    // 🔨 Deep autonomous worker: explores, understands, implements
    "hephaestus": {
      "model": "openai/gpt-5.3-codex"
    },

    // 🎨 Analyzes screenshots, PDFs, diagrams
    "multimodal-looker": {
      "model": "openrouter/moonshotai/kimi-k2.5"
    },

    // 🎨 Frontend/React/UI — visual programming is its forte
    "frontend-ui-ux-engineer": {
      "model": "openrouter/moonshotai/kimi-k2.5"
    },

    // ============================================================
    // TIER 3: EXECUTORS — high volume, cost optimized
    // MiniMax M2.5: $0.15/$1.20, 80.2% SWE-Bench, fast
    // ============================================================

    // 📚 Research docs: high volume, read-only
    "librarian": {
      "model": "gpt-5.1-codex-mini"
    },

    // 🔍 Contextual grep: runs 2-5 in parallel
    "explore": {
      "model": "openai/gpt-4.1-mini"
    },

    // 📝 Code review: repetitive, doesn't need a top model
    "momus": {
      "model": "gpt-5.1-codex-mini"
    },

    // ⚙️ Executes already-planned tasks
    "atlas": {
      "model": "openrouter/xiaomi/mimo-v2-flash"
    },

    // 📄 Docs and technical writing
    "document-writer": {
      "model": "openai/gpt-5.2"
    }
  },

  "categories": {
    // Frontend/UI → Kimi (multimodal + visual programming)
    "visual-engineering": {
      "model": "openrouter/moonshotai/kimi-k2.5"
    },
    // Hard logic → GPT-5.2 (best reasoning)
    "ultrabrain": {
      "model": "openai/gpt-5.2"
    },
    // Autonomous deep work → GPT-5.3-Codex (best agentic coding)
    "deep": {
      "model": "openai/gpt-5.3-codex"
    },
    // Creativity
    "artistry": {
      "model": "openai/gpt-5.3-codex"
    },
    // Trivial tasks
    "quick": {
      "model": "openrouter/xiaomi/mimo-v2-flash"
    },
    // Documentation
    "writing": {
      "model": "openai/gpt-5.2"
    },
    // Catch-all low effort → MiniMax
    "unspecified-low": {
      "model": "openrouter/minimax/minimax-m2.5"
    },
    // Catch-all high effort → GPT-5.2 (max reasoning for unclassified tasks)
    "unspecified-high": {
      "model": "openai/gpt-5.3-codex"
    }
  },
}

