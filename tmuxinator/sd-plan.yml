# ~/.tmuxinator/sd-plan.yml

name: sd-plan
root: ~/src/superdesk-planning

windows:
  - code: nvim
  - logs:
      layout: even-vertical
      panes:
        - cd ~/src/superdesk/server && honcho start
        - cd ~/src/superdesk/client && npx grunt
  - bash: git status
