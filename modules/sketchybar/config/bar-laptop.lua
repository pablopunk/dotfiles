local colors = require "colors"

-- Equivalent to the --bar domain
sbar.bar {
  height = 32,
  color = colors.bar.bg,
  padding_right = 2,
  padding_left = 2,
  corner_radius = 100,
  y_offset = 4,
  margin = 7,
}
