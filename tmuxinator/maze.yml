# ~/.tmuxinator/maze.yml

name: maze
root: ~/src/maze/maze-monorepo/

windows:
  - main: pnpm bootstrap && pnpm dev
  # - lambda: cd lambdas/project_service/ && pnpm serve:watch -- --stage testing
  - zsh: code . && git status
