source ~/.bash_functions
source ~/.bash_aliases
~/.bash_hello
F=$HOME/.bash_additional; [ -f $F ] && . $F # aditional local config

# Vars
export PATH="$PATH:$HOME/.local/bin"
export PATH="$PATH:/usr/local/bin"
export PATH="$PATH:$HOME/.bin"
export PATH="$PATH:$HOME/.npm-global/bin"
export PATH="$PATH:$HOME/.cargo/bin"
export PATH="$PATH:$HOME/.deno/bin"
export EDITOR=nvim
export HISTSIZE=
export HISTFILESIZE=
export LC_ALL=es_ES.UTF-8
export LANG=es_ES.UTF-8

function weather {
  curl -s wttr.in/${1-pontevedra}?format=1
}

if [ -n "$SSH_CLIENT" ] || [ -n "$SSH_TTY" ]; then
  pre='(ssh:'$(hostname)') '
fi

function clock {
  date "+%H:%M:%S"
}

# Use bashy if it exist
if hash bashy 2>/dev/null; then
  export PS1='\[\e[32m\]$(clock)\[\e[39m\] ${pre}$(bashy -r) '
else
  export PS1='\[\e[32m\]$(clock)\[\e[39m\] ${pre}$ '
fi

# Bash completion
[ -f /etc/bash_completion ] && . /etc/bash_completion
[ -f /usr/local/etc/bash_completion ] && . /usr/local/etc/bash_completion
[[ -r "/usr/local/etc/profile.d/bash_completion.sh" ]] && . "/usr/local/etc/profile.d/bash_completion.sh"
# git completion (branches and stuff) for mac
[ -f /Library/Developer/CommandLineTools/usr/share/git-core/git-completion.bash ] && . /Library/Developer/CommandLineTools/usr/share/git-core/git-completion.bash

# Disable flow control commands (keeps C-s from freezing everything)
stty start undef
stty stop undef
