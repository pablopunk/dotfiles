# ~/.tmuxinator/maze.yml

name: maze
root: ~/src/maze/maze-monorepo/

windows:
  - main: yarn dev
  - lambda: cd lambdas/project_service/ && yarn run serve:watch # --stage testing
  - zsh: code . && git status
