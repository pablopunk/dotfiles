function powersave {
  # linux
  sudo cpufreq-set -g powersave && \
    sudo pm-powersave true
}

function power {
  sudo cpufreq-set -g performance && \
    sudo pm-powersave false
}

function gclone {
  git clone git@github.com:pablopunk/$1
}

function grebase {
  git fetch upstream && \
    git rebase upstream/master
}

function copy {
  [ "$(uname)" == "Linux" ] && xsel --clipboard --input
  [ "$(uname)" == "Darwin" ] && pbcopy
}
