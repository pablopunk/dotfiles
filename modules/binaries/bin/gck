#!/usr/bin/env bash

fzf_short() {
  fzf --height=10 --reverse "$@"
}

if [[ $# -eq 0 ]]; then
  all_branches=$( git branch --sort=-committerdate | sed 's/\*//g' | sed 's/ /\n/g' | sed '/^\s*$/d' | uniq)
  echo "$all_branches" | fzf_short | xargs -I {} git checkout {}
else
  git checkout "$@"
fi


