# ~/.tmuxinator/sd-fi.yml

name: sd-fi
root: ~/src/superdesk-fi

on_project_start: 'sd deps -fi && sd wipe && sd populate -fi'

windows:
  - code: nvim
  - logs:
      layout: even-horizontal
      panes:
        - cd server && honcho start
        - cd client && npx grunt
  - shell: git status
