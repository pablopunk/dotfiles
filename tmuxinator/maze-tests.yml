# ~/.tmuxinator/maze.yml

name: maze-tests
root: ~/src/maze/maze-monorepo/

pre_window: nvm use 14

windows:
  - dev: pnpm dev:acceptance-tests
  - runner: cd packages/maze-test-suite && pnpm start-redis && pnpm debug
