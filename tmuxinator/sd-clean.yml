# ~/.tmuxinator/sd.yml

name: sd
root: ~/src/superdesk-client-core

on_project_start: 'sd deps && sd wipe && sd populate'

windows:
  - code: nvim
  - logs:
      layout: even-vertical
      panes:
        - cd ~/src/superdesk/server && honcho start
        - npx grunt
  - shell: git status
